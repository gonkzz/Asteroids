This is an Asteroids game built with the pygame module
To execute the game from the terminal:
  - first create a virtual environment "python3 -m venv venv"
  - then activate it "source venv/bin/activate"
